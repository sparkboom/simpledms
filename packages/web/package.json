{
  "name": "@sparkboom-smds/web",
  "version": "1.0.0",
  "description": "Simple DMS Web service",
  "author": "Matthew McLeod <matt@mattmcleod.co.uk>",
  "homepage": "https://github.com/sparkboom/simpledms#readme",
  "license": "ISC",
  "main": "src/index.ts",
  "directories": {
    "src": "src",
    "test": "__tests__"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sparkboom/simpledms.git"
  },
  "scripts": {
    "start": "deno run --importmap=import_map.json --allow-net --allow-write --allow-read --unstable mod.ts",
    "start:dev": "denon run --importmap=import_map.json --inspect --allow-net --allow-write --allow-read --unstable mod.ts",
    "start:debug": "denon run --importmap=import_map.json --inspect-brk --allow-net --allow-write --allow-read --unstable mod.ts",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "bugs": {
    "url": "https://github.com/sparkboom/simpledms/issues"
  }
}
